name: Publish

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: npm-release
    env:
      HUSKY: 0

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.29.3
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: "https://registry.npmjs.org"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Run release checks
        run: pnpm run check

      - name: Verify packed contents
        run: pnpm run pack:check

      - name: Verify tag matches package version
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: |
          PACKAGE_VERSION=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")
          EXPECTED_TAG="v${PACKAGE_VERSION}"
          if [ "$TAG_NAME" != "$EXPECTED_TAG" ]; then
            echo "Expected tag ${EXPECTED_TAG} but got ${TAG_NAME}" >&2
            exit 1
          fi

      - name: Publish to npm
        run: |
          PACKAGE_NAME=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).name")
          PACKAGE_VERSION=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")
          ACCESS_FLAG=""
          TAG_FLAG="--tag latest"
          if printf '%s' "$PACKAGE_NAME" | grep -q '^@'; then
            ACCESS_FLAG="--access public"
          fi
          if printf '%s' "$PACKAGE_VERSION" | grep -q -- '-'; then
            DIST_TAG=$(node -p "const version = JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version; version.split('-')[1].split('.')[0]")
            TAG_FLAG="--tag ${DIST_TAG}"
          fi
          npm publish --provenance $TAG_FLAG $ACCESS_FLAG
